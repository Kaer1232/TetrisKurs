<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TetrisKurs.View.ChoiceDifficultyContentView">

    <VerticalStackLayout>
        <Button Text="Легко"  Command="{Binding EasyBtmCommand}"/>
        <Button Text="Средне" Command="{Binding MiddleBtmCommand}"/>
        <Button Text="Тяжело" Command="{Binding HardBtmCommand}"/>
        <Button Text="Назад" Command="{Binding BackBtmCommand}"/>
    </VerticalStackLayout>
    
</ContentView>
